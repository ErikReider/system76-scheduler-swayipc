# System76 Scheduler SwayIPC

Uses SwayIPC to set the foreground process for the System76 Scheduler

## Running

Make sure that `system76-scheduler` is running. Then simply run `system76-scheduler-swayipc`
